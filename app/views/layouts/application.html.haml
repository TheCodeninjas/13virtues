!!! 5
%html
  %head
    %title RottenPotatoes!
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    = csrf_meta_tags
 
  %body
    - if flash[:msg]
      =flash[:msg]

    - if session[:user]
      =link_to 'Logout', '/logout'#logins_path, :method=>:destroy

    #main
      = yield
